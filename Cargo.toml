[package]
name = "n_body_problem"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bracket-geometry = "0.8.3"
ultraviolet = {version = "0.9.0", features = ["int", "serde"]}
